declare function Theme({ theme }: {
    theme: any;
}): JSX.Element;
declare namespace Theme {
    namespace propTypes {
        const theme: PropTypes.Validator<string>;
    }
}
export default Theme;
import PropTypes from "prop-types";
