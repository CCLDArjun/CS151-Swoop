export default Test;
declare class Test extends React.Component<any, any, any> {
    static propTypes: {
        theme: PropTypes.Validator<string>;
    };
    constructor(props: any);
    state: {
        placeholderButtonText: string;
        isDisabled: boolean;
    };
    render(): JSX.Element;
}
import React from "react";
import PropTypes from "prop-types";
